# Database Configuration
POSTGRES_DB=dsagent
POSTGRES_USER=dsagent
POSTGRES_PASSWORD=your_secure_password_here

# Server Configuration
NODE_ENV=production
PORT=3000
CORS_ORIGIN=https://ds-agent.alpy.io

# Frontend URLs (for production build)
VITE_API_URL=https://ds-agent.alpy.io/api
VITE_WS_URL=https://ds-agent.alpy.io

# Optional: Figma API (if using direct Figma API in future)
# FIGMA_ACCESS_TOKEN=your_figma_token_here
